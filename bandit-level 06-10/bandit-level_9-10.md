# Bandit Level 9 → Level 10

## Level Goal
The password for the next level is stored in the file data.txt in one of the few human-readable strings, preceded by several ‘=’ characters.

## Approach
To find, here we have to use strings command. to prints only the human readable contents.

## Commands Used
all basic commads.
strings -> it will prints the human readable format (contents) in the file.

## Solution
to find password that is human readable.
use  cmd : strings data.txt

password: FGUW511LVJrxX9kMYMmlN4MgbpfMiqey

## Explanation
To get the human readble content from the file data.txt we need to use strings command with filename.
searched in filtered contents i got the password also that preceeds many '=' symbols.
